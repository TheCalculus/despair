# external definition of puts
extern i32 @puts(ptr u8)

# define u8 pointer
ptr u8 @str = "hello world" 0

# main function
# void -> i32
i32 @main() {
# set vr0 to retval of puts
    %0 = i32 @puts(ptr @str)
# set vr1 to bool %0 > 0
    %1 = i32 eq %0, 0
# return vr1
    ret %1
}
